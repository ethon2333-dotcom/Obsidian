# 知识拓展 Loop · 广度辐射 —— 执行记忆

## 运行约定（口径，后续轮次沿用）

- 轮换状态文件：`C:\Users\Ethon\ObsidianVault\.workbuddy\memory\知识拓展_loop_state.md` —— **每轮必读**，里面有「已覆盖」「下轮候选」「已覆盖主题黑名单」。
- 库内已有 89+ 篇笔记，**重复立题是最大风险**。选题前务必 glob `01-笔记/**/*.md` 比对，并查黑名单。
- 4 个子代理并行（Agent 工具 general-purpose），每个 1 主题、1 文件；**子代理一律不许改 MOC**，MOC 由主代理串行统一更新，避免并发写冲突。
- 给子代理的 prompt 必须附 **wikilink 白名单**（从实际文件名生成），否则会造死链。收尾用脚本校验：提取 `[[...]]` 与实际文件名做 `comm -23` 比对。
- 笔记规格：YAML frontmatter → 学习定位 → 一句话心智模型 → 大量表格铺广度 → 「对 OS PM 的意义」→ `- [ ]` 待解问题 → 来源清单表 → ⚠️ 待核实清单 → 底部 `#标签/...`。
- 诚实纪律执行到位：本轮明确要求"宁可不写数字也不编造"，Agent 评测那篇因榜单数字互相矛盾而**刻意全篇不写 SOTA 百分比**，做法正确，后续沿用。

## 执行记录

### 2026-08-09 · 第 1 轮（首次运行）

- 状态文件不存在 → 初始化，并额外加了「下轮候选」与「已覆盖黑名单」两节，减少后续轮次的选题成本。
- 本轮 4 篇，刻意覆盖四大锚点各一：Agent 评测（AI Agent）／移动端 NPU 编译栈（OS 端侧）／PKM+Obsidian（效率工具）／Agentic UX（PM 交互设计）。
- **重要发现**：「效率工具/知识管理/Obsidian」是四大锚点里**唯一零覆盖**的——89 篇笔记无一篇讲方法论本身。已新建 `01-笔记/知识管理与效率工具/` 子文件夹 + MOC 补洞。
- 主动避坑：原计划写「ANN 近似最近邻」，比对后发现 `向量数据库 学习笔记` 第四节已覆盖，临时换题为 Agent 评测。
- 收尾校验：4 篇共 1530 行，死链检查 0 条。MOC 已注册 4 处（AI Agent 框架 MOC / PM决策层 MOC / OS产品经理知识库 MOC / 新建 知识管理与效率工具 MOC）。
- 未触碰 `10-知识飞轮/`，符合约束。

### 2026-08-10 · 第 2 轮

- 4 篇：端侧 SLM 生态（AI模型基础）／语音 ASR·TTS（手机AI智能体）／隐私计算与端云协同（安全）／AI 眼镜与可穿戴【热点】（发散图谱）。共 1164 行。
- 落点刻意与第 1 轮错开，四个不同子文件夹；本轮未覆盖 PM 锚点，已在状态文件把「AI 产品度量」标为下轮**优先**。
- **补洞**：`AI模型基础/` 是全库唯一无 MOC 的子文件夹（6 篇裸放），已新建 `AI模型基础 MOC.md`，按「认知→选型→落地」三层组织。
- **流程改进（沿用）**：白名单机制仍有 1 处漏网——子代理把文件夹名 `[[AI模型基础]]` 当笔记链了。原因是白名单里我误放了这个非笔记条目。**教训：白名单必须严格由实际 .md 文件名生成，不能手工添加目录名。** 校验脚本抓到了，已修为 `[[AI模型基础 MOC]]`。
- 诚实纪律成效显著：语音篇因检索到 5 组互相冲突的延迟口径而**显式说明"为什么这里不给数字"**；眼镜篇在玩家表加了「信息性质（官方/传闻）」列，标出 7 条传闻·未证实。这两个做法后续沿用。
- 收尾校验：死链 0 条；4 篇各 10 条 `- [ ]` 待解问题；MOC 注册 3 处 + 新建 1 处。未触碰 `10-知识飞轮/`。

### 2026-08-15 · 第 3 轮

- 4 篇，锚点错开：PM（数据飞轮与 AI 产品度量）／效率工具（AI 编程助手与 Agentic Coding 生态）／发散图谱（智能座舱与车机 HMI 意图入口）／AI模型基础（合成数据与模型后训练）。刻意让每一篇都从已覆盖主题外辐射一跳。
- 选题避坑：本轮 4 题全部不在黑名单；「合成数据与模型后训练」刻意区分于已覆盖的「端侧量化/端侧推理」（讲训练侧 vs 推理侧）。
- 子代理纪律到位：语音/眼镜轮遗留的「冲突口径显式标注」做法延续——合成数据篇把 Gartner 80% 预测与具体 MMLU 分值全标「待核实」；座舱篇把 6 条传闻显式标「传闻·未证实」并标注骁龙 TOPS 口径冲突。未编造数字。
- 主代理流程沿用：4 子代理并行（各 1 文件，严控不碰 MOC）→ 主代理串行读 MOC 注册 4 处 → 死链校验脚本（wikilink vs 实际文件名，`comm`-式比对）→ 0 死链（库内 103 篇）。状态文件追加本轮 + 刷新下轮候选 + 扩展黑名单。
- 未触碰 `10-知识飞轮/`，符合约束。

### 2026-08-15 · 第 4 轮（当日第二次运行）

- 4 篇，分属四个不同子文件夹/锚点：AI模型基础（模型蒸馏与师生训练）／手机AI智能体（多模态 GUI 理解与 UI Grounding）／发散图谱（无障碍 Accessibility 与 GUI Agent 同源技术栈）／PM决策层（时序/事件驱动与 Agent 主动服务）。
- 选题避坑：4 题全部不在黑名单；刻意与已覆盖的「合成数据/后训练」「端侧 SLM」「GUI Agent 架构」「端侧意图框架」做差异化（蒸馏=迁移侧 vs 训练侧；GUI 理解=算法/grounding 侧 vs 架构侧）。
- 流程沿用：4 子代理并行（各 1 文件，严控不碰 MOC）→ 主代理串行读 4 个 MOC 注册 → 死链校验脚本（wikilink vs 实际文件名）→ 0 死链（库内 107 篇）。状态文件追加本轮 + 刷新下轮候选 + 扩展黑名单。
- 诚实纪律：子代理对冲突口径（DistilBERT 97% / TCO 70% / ScreenSpot-Pro 18.9% vs 61.6% 等）显式标「待核实」，未编造数字。
- 未触碰 `10-知识飞轮/`，符合约束。

### 2026-08-16 · 第 5 轮（广度辐射）

- 4 篇，4 个不同子文件夹/锚点：手机AI智能体（端侧多模态 VLM）／AI模型基础（端云协同推理与混合部署）／PM决策层（意图框架开发者生态与平台经济）／AI Agent 框架（多智能体协作与编排）。
- 选题：4 题全部不在黑名单，均为锚点外辐射一跳；刻意回避 `Agent 记忆与个性化意图理解.md`（已存在）因此没立「Agent 记忆系统」题，改立「开发者生态与平台经济」从 PM 锚点外辐射。
- 流程沿用：4 子代理并行（各 1 文件，严控不碰 MOC）→ 主代理串行读 4 个 MOC 注册（手机AI智能体知识库 / AI模型基础 MOC / PM决策层 MOC / AI Agent 框架 MOC）→ 死链校验脚本（wikilink vs 实际文件名，111 个有效目标）→ 0 死链。状态文件追加本轮 + 刷新下轮候选 + 扩展黑名单。
- 诚实纪律：子代理对冲突口径（MiniCPM-V 参数量 2.6B vs 8B / Apple「95% 端侧」第三方口径 / Gemini Nano 内存 25–100MB / RouteLLM 85% / Bedrock「94%」vs 官方 ~30% 等）显式标「待核实」，未编造数字。
- 未触碰 `10-知识飞轮/`，符合约束。

### 2026-08-17 · 第 6 轮（广度辐射）

- 4 篇，4 个不同子文件夹/锚点：手机AI智能体（端侧 AI 功耗与散热约束）／AI Agent 框架（Agent 可观测性 LLM Observability）／知识管理与效率工具（低代码无代码 Agent 搭建平台）／安全（端侧模型安全与越狱）。
- 选题：4 题全部不在黑名单，均为锚点外辐射一跳——功耗（OS 端侧独立维度，区别于 NPU 编译栈/端侧推理落地）／可观测性（AI Agent 生产监控，区别于 Agent 评测基准/工具调用安全）／低代码（效率工具非开发者搭建，区别于 AI 编程助手）／端侧安全（安全锚点，区别于工具调用安全/隐私计算）。
- 流程沿用：4 子代理并行（各 1 文件，严控不碰 MOC）→ 主代理串行读 4 个 MOC 注册（意图框架·跨体系索引 MOC / AI Agent 框架 MOC / 知识管理与效率工具 MOC / 安全 MOC）→ 死链校验脚本（wikilink vs 实际文件名，落地于 `automation-1785994253815/deadlink_check.py`）→ 0 死链（4 篇共 26 条 wikilink）。
- 诚实纪律：子代理对冲突口径显式标「待核实」——高通 Hexagon TOPS 45/73/75/80 冲突、Apple A19 Pro「3×」媒体口径、OTel GenAI 稳定态、Coze 插件 100+ vs 1万+、Dify Marketplace 8677 vs 600、EU AI Act 参数阈值；未编造具体数字。
- 未触碰 `10-知识飞轮/`，符合约束。库内笔记现约 115 篇。
