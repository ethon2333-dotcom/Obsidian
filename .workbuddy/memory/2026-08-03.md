
## 21:00 AppIntent 每日情报（晚间增补跑）

- 同日二次触发，改为**增补模式**：只记 09:00 版之后净新增，文件名加 `-晚` 后缀，显式列出「已复核·无净新增」清单。此模式建议固化为同日重复触发的默认做法。
- 净新增 2 条：Bonsai 1-bit 量化 BFCL 实测（7/10）、银联 APOP/AVOP 里程碑（6/10）。
- 产出：A `AppIntent 每日情报 2026-08-03-晚.md`、B 净新增 `意图支付授权协议 APOP.md`、B 增补 `Function Calling 端侧工具调用.md`、C 修订 `系统级 Intent 路由评估 SOP.md`、D `AppIntent 每日情报速览 2026-08-03-晚.md`，看板已登记。
- 库内口径修正：SOP 中残留的 FunctionGemma「46%→90%」已改为官方「58%→85%」，与 B 层保持一致。

## Vault 全量深耕循环 · 第 1 轮（vault-deepen，批量 8）

- 用户要求「全库 137 篇一直循环深耕直到手动停止」。已建进度台账 `.workbuddy/memory/vault_deepen_progress.md`（142 项，P1–P5 优先级）与技能 `vault-deepen`。
- 本轮处理 P1 B-泛化概念前 8 篇：6 篇补齐 `[[意图框架·跨体系索引 MOC]]` 链接 + `反例与边界` 小节；Android AppFunctions / Apple AppIntents 此前已完整（自带最新来源+反例+开放问题+MOC 链接），仅标记 done。
- 强化 MOC：新增 `Context Engineering 学习笔记`、`端侧大模型推理 学习笔记` 两行前向索引。
- 累计 done 8/142；下一轮从 `Atomic Service 元服务` 续。EBUSY 偶发（Obsidian 锁），重试即过。
